# BLEUnlock 上游 Issues / PR 改进分析

> 本文档基于上游仓库 **[ts1/BLEUnlock](https://github.com/ts1/BLEUnlock)** 的 Issues 与 Pull Requests 整理。
> 当前仓库 fork 自 ts1/BLEUnlock（经 Skyearn 的活跃分支），目的是找出对本项目代码有借鉴/改进价值的点。
> 分析对象：`BLEUnlock/AppDelegate.swift`、`BLEUnlock/BLE.swift`、`BLEUnlock/lowlevel.c` 等当前源码。
> 数据抓取时间：2026-07-27。

---

## 一、结论速览（按优先级）

| 优先级 | 改进主题 | 来源 | 现状 |
| :--- | :--- | :--- | :--- |
| 🔴 高 | 新版 macOS 解锁失效（Sequoia/26 下 `fakeKeyStrokes` 打不进登录框） | Issue #140, #179, PR #179 | 未修复 |
| 🔴 高 | 蓝牙扫描不到设备 / 需关开蓝牙或重启才恢复 | Issue #44, #45, #76, #82 | 未系统性修复 |
| 🔴 高 | 误解锁：单次 RSSI 突变导致隔房自动解锁（安全风险） | PR #184(#153), #189, #183 | 未合并 |
| 🟠 中 | BLE 断连/掉线恢复（iPhone 锁屏断连、无自动重扫） | Issue #78, PR #183 | 未合并 |
| 🟠 中 | 多设备监控 + 设备身份/名称解析 | PR #179 | 单设备 |
| 🟠 中 | 记住已选设备（设备被"遗忘"） | Issue #96 | 依赖 UUID 易失效 |
| 🟡 低 | 设备名显示 / 自定义重命名 / 去重 | Issue #58, PR(Rename), PR(Duplicate) | 未合并 |
| 🟡 低 | 功能增强：Sleep RSSI、Wi-Fi 暂停、Telegram 告警、隐藏图标 | 多个 PR | 未合并 |
| 🟡 低 | 脚本 Bug、本地化、brew 分发 | PR(parse_info)、多语言 PR | 部分缺失 |

---

## 二、核心问题域详解

### 1. 🔴 新版 macOS 自动解锁失效（最值得优先处理）

**相关：** Issue [#140 macOS 15 Sequoia compatibility]、PR [#179 macOS 26 compatibility]

- 用户反馈：升级到 macOS 15 后，**息屏/锁屏唤醒后无法自动输入密码解锁**，怀疑锁屏界面变化导致 `fakeKeyStrokes` 未被接收，或时序错位。
- 代码定位：`AppDelegate.swift` 的 `tryUnlockScreen()` → `fakeKeyStrokes()`，用 `CGEvent` + `keyboardSetUnicodeString` post 到 `.cghidEventTap`。
- 根因分析：
  1. **时序问题**：新系统锁屏 → 唤醒 → 登录框出现之间存在延迟，固定的 `0.5s` 延时（`Timer.scheduledTimer(withTimeInterval: 0.5)`）可能不足；需要在登录框真正就绪后再打字（或重试多次）。
  2. **SecureEventInput 拦截**：新系统对锁屏/登录窗合成键盘事件的限制更严，`.cghidEventTap` 合成事件可能被丢弃。
  3. **屏保 vs 锁屏路径差异**：`inScreensaver` 分支先发 Esc 唤出登录框，但直接锁屏路径没有等待登录框出现的确认。
- **建议改进：**
  - 打字前**轮询确认登录框已就绪**（多次重试 + `isScreenLocked()` 校验），而非单次固定延时。
  - 拆分"唤醒显示器"与"输入密码"两个阶段，唤醒后重试若干次直到会话可接收输入。
  - 兜底：无法解锁时给出明确提示（而非静默失败），引导用户用系统"锁定屏幕宽限期"或 Apple Watch 邻近解锁。

> 📌 **对本项目/NearLock 的直接价值**：这与 NearLock 遇到的"CGEvent 打不进真锁屏"是同一难题。ts1 上游至今未解决，说明"往真锁屏打字"在新系统上不可靠，应把稳定方案落在**唤醒即解锁（宽限期）**上。

---

### 2. 🔴 蓝牙扫描不到设备 / 需关开蓝牙才恢复

**相关：** Issue [#44 not work on Monterey]、[#45 Failed to get Bluetooth device list]、[#76 only shows scanning]、[#82 only works when Mac restarted]

- 现象集合：
  - Monterey 起频发"一直 Scanning、扫不到任何设备"，**关开蓝牙 / 重启 Mac 才恢复**。
  - **登出/登录或睡眠唤醒后 app 变为 inactive，不再扫描**。
  - 进入 `lost -50` 状态后设备列表卡在 Scanning，`CBCentralManager` 未恢复。
- 代码定位：`BLE.swift` 的 `startScanning()` / `centralManagerDidUpdateState`；`AppDelegate.swift` 的 `onSystemWake` / `onSystemSleep`（切换 `setActivationPolicy` 以便蓝牙恢复后可扫描）。
- **建议改进：**
  - 监听 `CBCentralManager` 的 `.poweredOff → .poweredOn` 状态迁移，恢复后**自动重启扫描**（当前恢复逻辑不完整）。
  - 增加"看门狗"：一定时间内无任何广播回调则主动 `stopScan → startScan` 重置。
  - **记录 Active/Inactive 状态转换日志**（Issue #45 用户明确建议），便于诊断"何时掉出扫描"。
  - 系统唤醒后延时重扫（`onSystemWake` 已有 1s 延时，可加重试）。

---

### 3. 🔴 误解锁：单次 RSSI 突变导致自动解锁（安全风险）

**相关：** PR [#184 Fix false-positive unlocks (refs #153)]、PR [#189 Improve proximity detection stability]、PR [#183 Improve BLE recovery and RSSI stability]

- 问题：解锁判定使用**单次原始 RSSI**，一次偶发强信号（多径反射、广播突发）就可能"隔着房间"解锁 Mac。
- 代码定位：`BLE.swift` 的 `getEstimatedRSSI()`（`vDSP_normalizeD` 求最近 5 点均值）与 presence/unlock 判定。
- 三个 PR 的思路一致且互补：
  - **先做移动平均再判定解锁**，并要求**最小样本数**（避免刚连上仅 1~2 个样本就解锁）。
  - 加入 proximity 确认引擎：收集一小段连续读数，要求**结果稳定一致**才确认靠近。
  - **忽略无效 RSSI 读数**、处理读失败/蓝牙不可用、防止并发 proximity 检查。
- **建议改进：** 采纳"平滑 + 最小样本数 + 稳定性确认"用于**解锁方向**（锁定方向可更激进以保安全）。这是安全性收益最高、改动可控的一项。

---

### 4. 🟠 BLE 断连恢复（iPhone 锁屏导致掉线）

**相关：** Issue [#78 loses connection when iPhone screen locks]、PR [#183 Improve BLE recovery]

- 现象：iPhone 锁屏时与 Mac 间歇性断连；多台 Mac 场景下解锁一台会连带解锁另一台。
- PR #183 提供的做法：
  - 补全 **CoreBluetooth 断连 / 连接失败回调**。
  - 断连或蓝牙电源恢复后**清理过期连接定时器并重启扫描**。
  - 配合 RSSI 平滑与最小样本（与第 3 项同源）。
- **建议改进：** 完善断连回调链路 + 自动重扫，避免掉线后停留在旧状态。

---

### 5. 🟠 多设备监控 + 设备身份/名称解析（fork 主线方向）

**相关：** PR [#179 Add macOS 26 compatibility improvements and multi-device support]（由 Skyearn 提交，即本 fork 的上游）

PR #179 的完整改动清单（**本 fork 应重点参考/合入**）：
- 支持**同时监控多个 BLE 设备**（多个可信设备）。
- 更新 lock/unlock 逻辑以适配多设备流程。
- 改进设备名解析，增加更好的 **fallback**。
- **过滤 `N/A` 等占位/无意义名称**。
- 仅有部分设备信息时改进 Apple 设备命名。
- 处理**旋转/随机化的 BLE 标识符**，减少"被监控设备永久丢失"。
- 更新 fork 的文档与 release workflow。
- 局限：Android 等隐私设备的 BLE 身份仍会旋转，无法完全消除 UUID 回退。

---

### 6. 🟠 记住已选设备（设备被"遗忘"）

**相关：** Issue [#96 Save selected device]

- 问题：选定设备离开并连接过其它 BLE 设备后再回来，BLEUnlock"忘记"了已选设备，需要重新扫描选择。
- 代码定位：`AppDelegate.swift` 用 `prefs.set(uuid.uuidString, forKey: "device")` 保存的是 **CoreBluetooth UUID**，该 UUID 在某些情况下会变，导致失配。
- **建议改进：** 优先用 **MAC 地址**（`device.macAddr`，代码已从 `com.apple.Bluetooth.plist` 读取）作为持久标识兜底，UUID 失效时按 MAC 重新绑定。

---

### 7. 🟡 设备名显示 / 自定义重命名 / 去重

- Issue [#58 Display Bluetooth name instead of device name]：多台同型号 iPhone，希望按用户改的蓝牙名显示以便区分。
- PR **Add custom device names via Rename Device menu item**：新增"Rename Device…"菜单，自定义名按 UUID 存 `UserDefaults`，在扫描列表与顶部读数显示（如 `My iPhone: −65dBm`）。
- PR **Fix: Duplicate device names appear in the device list**：修复设备列表出现重复名。
- **建议改进：** 合入自定义重命名 + 列表去重，显著改善多设备可用性。

---

### 8. 🟡 功能增强类 PR（可选，按需取用）

| PR | 功能 | 价值 |
| :--- | :--- | :--- |
| **Add Sleep RSSI Feature**（close #153） | 新增 Sleep RSSI 阈值菜单：信号弱到一定程度只**息屏**而非锁定 | 减少不必要的锁屏 |
| **Add Wi-Fi auto-pause (default-gateway MAC)**（#142） | 在家庭网络下**暂停**自动锁/解锁，用网关 MAC 识别，**免 SSID/定位权限** | 居家免打扰，隐私友好 |
| **Add optional Telegram alerts** | 离开/丢信号/解锁事件推送 Telegram，可选**拍照 + GPS 定位**，token 存 Keychain | 防盗/安全告警 |
| **Add hide menu bar icon option** | 隐藏菜单栏图标，重开时恢复 | 界面精简 |
| **Add toggle to run event script while locked** | 已锁屏时也运行 event 脚本 | 自动化扩展 |

> ⚠️ Telegram + 拍照 + 定位涉及相机/定位权限与隐私合规，若上架需谨慎（PR 已做成默认关闭 + 显式 opt-in）。

---

### 9. 🟡 Bug 修复 / 本地化 / 分发

- **PR Fix incorrect variable reference in `parse_info()`**：`apple-device-names` 脚本中 `parse_info()` 用了外层变量 `f` 而非参数 `filename`，独立调用会 `NameError`。**纯 Bug 修复，建议直接合入**。
- **本地化**：Traditional Chinese README、French、Norwegian 翻译修正 —— 低风险文档/字符串改动。
- **Issue #? add BLEUnlock to [home]brew**：希望 `brew install --cask bleunlock` 分发。可考虑提交 homebrew-cask。
- **Android MAC 随机化 / Huawei 找不到**（Issue #24 等）：安卓设备 BLE 地址随机化导致无法稳定识别，属平台限制，可通过"配套 Android app 广播固定标识"缓解（PR #179 也提到）。

---

## 三、针对本 fork 的落地建议（优先级排序）

1. **合入上游 PR #179**（本 fork 的直接上游主线）：多设备 + 设备名解析 + macOS 26 兼容 + 旋转标识处理。
2. **采纳 RSSI 平滑 + 最小样本数解锁**（PR #184/#189/#183）：安全收益最高、改动可控。
3. **补全 BLE 断连回调与蓝牙恢复后自动重扫**（PR #183）：解决"扫不到/掉线卡死"。
4. **解锁时序健壮化**（Issue #140）：打字前轮询确认登录框就绪 + 重试，失败给明确提示。
5. **设备持久标识用 MAC 兜底**（Issue #96）：避免 UUID 变化导致"忘记设备"。
6. **合入低风险改进**：`parse_info()` Bug 修复、自定义重命名、列表去重、本地化。
7. **按需选配增强**：Sleep RSSI、Wi-Fi 暂停、隐藏图标、Telegram 告警。

---

## 附：数据来源清单

**Issues（部分，含混入的 PR）：**
`#44 not work on Monterey`(closed) · `#45 Failed to get Bluetooth device list`(closed) · `#76 only shows scanning`(open) · `#24 Can not find Huawei phone`(closed) · `#47 Keep getting flashbacks`(closed) · `MBP/iMac loses connection when iPhone locks`(open) · `Bluetooth can't see any device`(open) · `#58 Display Bluetooth name`(open) · `Project Abandoned? fast user switching`(open) · `only works when Mac restarted`(closed) · `Android MAC randomization`(open) · `#96 Save selected device`(open) · `add to brew`(closed) · `#179 macOS 26 + multi-device`(PR/open) · `#140 macOS 15 Sequoia`(open)

**Open Pull Requests：**
`#189 Improve Bluetooth proximity detection stability` · `#188 Add Traditional Chinese README` · `#187 Add optional Telegram alerts with photos and Mac location` · `#183 Improve BLE recovery and RSSI stability` · `Fix incorrect variable reference in parse_info()` · `Add Wi-Fi auto-pause keyed on default-gateway MAC (#142)` · `Fix false-positive unlocks: gate unlock on smoothed RSSI + minimum sample count (#153)` · `Add custom device names via Rename Device menu item` · `#179 Add macOS 26 compatibility improvements and multi-device support` · `Add hide menu bar icon option` · `French localization` · `Add toggle to also run the event script while locked` · `Improve norwegian translation` · `Fix: Duplicate device names appear in the device list` · `Add Sleep RSSI Feature (#153)`
